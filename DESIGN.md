# Metadata Generator Design

이 프로젝트는 유튜브 음악 채널용 메타데이터를 자동으로 생성하는 것을 목표로 합니다. `generate_metadata.py`는 이미지 파일명을 기반으로 다음 정보를 포함한 텍스트 파일을 생성합니다.

## 입력/출력 구조
- 입력: `input/background` 폴더의 이미지 파일
- 출력: `output/metadata` 폴더의 `*_metadata.txt` 파일

## 파일 매칭 규칙
- 이미지 파일명과 동일한 이름으로 메타데이터 파일 생성
- 예: `TEST.png` → `output/metadata/TEST_metadata.txt`

## 테마
다음 3가지 테마를 지원합니다.
- `FOCUS`
- `SLEEP`
- `DRIVE`

테마는 기본적으로 파일명에 포함된 키워드로 자동 감지합니다.
- 예: `focus_study.png` → `FOCUS`

테마를 명시적으로 지정하고 싶다면 `--theme` 옵션을 사용합니다.

## 메타데이터 구성
각 출력 파일은 아래 섹션으로 구성됩니다.
1. 제목 3안 (SEO / 감성 / 믹스)
2. 설명 (첫 2줄 후킹 포함)
3. 태그 20~30개
4. 고정댓글 템플릿

## 작성 원칙
- 제목은 테마별 핵심 키워드를 반영합니다.
- 설명은 첫 2줄에 강한 후킹 문장을 배치합니다.
- 태그는 20~30개 사이로 유지하며 중복을 제거합니다.
- 고정댓글은 재생목록 안내 + 구독/좋아요 유도 문구를 포함합니다.
